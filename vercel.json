{
  "version": 2,
  "functions": {
    "api/expenses.js": {} // Keep the file mapping, but remove the "runtime" property
  },
  "routes": [
    {
      "src": "/api/expenses",
      "dest": "/api/expenses.js",
      "methods": ["GET", "POST"]
    },
    {
      "src": "/.*",
      "dest": "index.html"
    }
  ]
}
